[project]
name = "portfolio-voice-agent"
version = "0.1.0"
description = "Voice agent that answers questions about Wasif Ullah, grounded in the portfolio's own llms.txt"
requires-python = ">=3.11,<3.13"
dependencies = [
    "livekit-agents[groq,silero]~=1.7",
    "python-dotenv>=1.0",
    "httpx>=0.27",
    "piper-tts>=1.3",
]

[tool.uv]
package = false
